public class dispositivo_preferido extends dispositivos{
    private int index;

    public dispositivo_preferido(String nombre, int index) {
        super(nombre);
        this.index = index;
    }
}
